const express=require("express");const Attendance=require("./attendanceModel");const User=require("../users/userModel");const protect=require("../../middleware/authMiddleware");const authorize=require("../../middleware/roleMiddleware");const {body}=require("../../validation");const {canManage,summary}=require("../../services/attendanceService");
const router=express.Router();router.use(protect);const statuses=["Present","Absent","Late","Excused"];
router.get("/",async(req,res,next)=>{try{let q={};if(req.user.role==='student')q.student=req.user._id;else if(req.user.role==='mentor'){const ss=await User.find({role:'student',mentor:req.user._id,status:'approved',isActive:true}).select('_id');q.student={$in:ss.map(s=>s._id)};}else if(req.query.studentId){q.student=req.query.studentId;}if(req.query.from||req.query.to)q.date={...(req.query.from?{$gte:new Date(req.query.from)}:{}),...(req.query.to?{$lte:new Date(req.query.to)}:{})};const records=await Attendance.find(q).populate('student','fullName email').populate('mentor','fullName').sort({date:-1});res.json({success:true,count:records.length,attendance:records});}catch(e){next(e);}});
router.get("/summary/:studentId",async(req,res,next)=>{try{if(!(await canManage(req.user,req.params.studentId))&&req.user.role!=='student')return res.status(403).json({success:false,message:'You are not authorized to view this student.'});if(req.user.role==='student'&&String(req.user._id)!==String(req.params.studentId))return res.status(403).json({success:false,message:'You can only view your own attendance.'});res.json({success:true,summary:await summary(req.params.studentId,req.query.from,req.query.to)});}catch(e){next(e);}});
router.post("/",authorize('admin','mentor'),body({studentId:{required:true,type:'objectId'},date:{required:true},status:{required:true,enum:statuses}}),async(req,res,next)=>{try{if(!(await canManage(req.user,req.body.studentId)))return res.status(403).json({success:false,message:'You can only manage assigned students.'});const d=new Date(req.body.date);if(Number.isNaN(d.getTime()))return res.status(400).json({success:false,message:'Invalid date.'});const r=await Attendance.findOneAndUpdate({student:req.body.studentId,date:d},{student:req.body.studentId,mentor:req.user._id,date:d,status:req.body.status,note:String(req.body.note||'').trim()},{new:true,upsert:true,runValidators:true});res.status(201).json({success:true,attendance:r});}catch(e){next(e);}});
router.patch("/:id",authorize('admin','mentor'),async(req,res,next)=>{try{const r=await Attendance.findById(req.params.id);if(!r)return res.status(404).json({success:false,message:'Attendance record not found.'});if(!(await canManage(req.user,r.student)))return res.status(403).json({success:false,message:'You can only manage assigned students.'});if(req.body.status!==undefined&&!statuses.includes(req.body.status))return res.status(400).json({success:false,message:'Invalid attendance status.'});if(req.body.date!==undefined){const d=new Date(req.body.date);if(Number.isNaN(d.getTime()))return res.status(400).json({success:false,message:'Invalid date.'});r.date=d;}if(req.body.status!==undefined)r.status=req.body.status;if(req.body.note!==undefined)r.note=String(req.body.note).trim();await r.save();res.json({success:true,attendance:r});}catch(e){next(e);}});
router.delete("/:id",authorize('admin','mentor'),async(req,res,next)=>{try{const r=await Attendance.findById(req.params.id);if(!r)return res.status(404).json({success:false,message:'Attendance record not found.'});if(!(await canManage(req.user,r.student)))return res.status(403).json({success:false,message:'You can only manage assigned students.'});await r.deleteOne();res.json({success:true,message:'Attendance deleted.'});}catch(e){next(e);}});
module.exports=router;
