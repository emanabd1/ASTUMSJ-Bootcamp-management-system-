const express=require('express');
const Batch=require('./batchModel');
const User=require('../users/userModel');
const protect=require('../../middleware/authMiddleware');
const authorize=require('../../middleware/roleMiddleware');
const router=express.Router();
router.use(protect,authorize('admin'));
const validDates=(startDate,endDate)=>startDate&&endDate&&!Number.isNaN(new Date(startDate).getTime())&&!Number.isNaN(new Date(endDate).getTime())&&new Date(endDate)>=new Date(startDate);
router.get('/',async(req,res,next)=>{try{const batches=await Batch.find().populate('mentors','fullName email').populate('students','fullName email department yearOfStudy').sort({startDate:-1});res.json({success:true,batches})}catch(e){next(e)}});
router.post('/',async(req,res,next)=>{try{const {name,description,startDate,endDate}=req.body;if(!name||!validDates(startDate,endDate))return res.status(400).json({success:false,message:'Name and valid start/end dates are required.'});const batch=await Batch.create({name,description,startDate,endDate});res.status(201).json({success:true,batch})}catch(e){next(e)}});
router.patch('/:id',async(req,res,next)=>{try{const batch=await Batch.findById(req.params.id);if(!batch)return res.status(404).json({success:false,message:'Batch not found.'});const {name,description,startDate,endDate,status}=req.body;if(name!==undefined)batch.name=String(name).trim();if(description!==undefined)batch.description=description;if(startDate!==undefined)batch.startDate=startDate;if(endDate!==undefined)batch.endDate=endDate;if(status!==undefined&&!['upcoming','active','completed'].includes(status))return res.status(400).json({success:false,message:'Invalid batch status.'});if(status!==undefined)batch.status=status;if(!validDates(batch.startDate,batch.endDate))return res.status(400).json({success:false,message:'End date must be on or after start date.'});await batch.save();res.json({success:true,batch})}catch(e){next(e)}});
router.delete('/:id',async(req,res,next)=>{try{const batch=await Batch.findByIdAndDelete(req.params.id);if(!batch)return res.status(404).json({success:false,message:'Batch not found.'});await User.updateMany({_id:{$in:batch.students}},{$unset:{batch:1}});res.json({success:true,message:'Batch deleted.'})}catch(e){next(e)}});
router.patch('/:id/mentors',async(req,res,next)=>{try{const batch=await Batch.findById(req.params.id);if(!batch)return res.status(404).json({success:false,message:'Batch not found.'});const ids=Array.isArray(req.body.mentorIds)?req.body.mentorIds:[];const mentors=await User.find({_id:{$in:ids},role:'mentor',status:'approved',isActive:true}).select('_id');batch.mentors=mentors.map(m=>m._id);await batch.save();res.json({success:true,batch})}catch(e){next(e)}});
router.patch('/:id/students',async(req,res,next)=>{try{const batch=await Batch.findById(req.params.id);if(!batch)return res.status(404).json({success:false,message:'Batch not found.'});const ids=Array.isArray(req.body.studentIds)?req.body.studentIds:[];const students=await User.find({_id:{$in:ids},role:'student',status:'approved',isActive:true}).select('_id');batch.students=students.map(s=>s._id);await batch.save();await User.updateMany({role:'student',_id:{$in:ids}},{$set:{batch:batch._id}});res.json({success:true,batch})}catch(e){next(e)}});
module.exports=router;
