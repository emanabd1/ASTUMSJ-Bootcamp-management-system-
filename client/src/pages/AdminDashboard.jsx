import React,{useEffect,useState} from 'react';import axiosInstance from '../api/axiosInstance';
export default function AdminDashboard(){const [stats,setStats]=useState(null),[error,setError]=useState('');useEffect(()=>{axiosInstance.get('/users/stats').then(r=>setStats(r.data.stats)).catch(e=>setError(e.response?.data?.message||'Could not load dashboard.'))},[]);if(error)return <p className="rounded-xl border border-rose-900 bg-[#1e1713] p-4 text-rose-300">{error}</p>;if(!stats)return <p className="text-[#a39081]">Loading dashboard...</p>;const cards=[['Students',stats.students],['Mentors',stats.mentors],['Batches',stats.batches],['Attendance',`${stats.attendancePercentage}%`],['Assignments',stats.assignments],['Pending Grading',stats.pendingGrading],['Submissions',stats.submissions],['Pending Applications',stats.pending],['Suspended',stats.suspended]];return <div className="space-y-7"><div><h1 className="text-3xl font-extrabold">Admin Dashboard</h1><p className="text-xs text-[#a39081]">Live bootcamp statistics from MongoDB.</p></div><div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5">{cards.map(([k,v])=><div key={k} className="rounded-2xl border border-[#4a3b32] bg-[#1e1713] p-5"><p className="text-xs text-[#a39081]">{k}</p><p className="mt-2 text-3xl font-bold text-[#c89b7b]">{v}</p></div>)}</div><section className="rounded-2xl border border-[#4a3b32] bg-[#1e1713] p-5"><h2 className="font-bold">Assignment Statistics</h2><div className="mt-4 grid grid-cols-2 gap-4 md:grid-cols-4"><div><p className="text-xs text-[#a39081]">Total</p><b>{stats.assignments}</b></div><div><p className="text-xs text-[#a39081]">Submitted</p><b>{stats.submissions}</b></div><div><p className="text-xs text-[#a39081]">Graded</p><b>{stats.graded}</b></div><div><p className="text-xs text-[#a39081]">Awaiting Grade</p><b>{stats.pendingGrading}</b></div></div></section><section className="rounded-2xl border border-[#4a3b32] bg-[#1e1713] p-5"><h2 className="font-bold">Recent Activity</h2><div className="mt-4 divide-y divide-[#4a3b32]">{stats.recentActivity?.length?stats.recentActivity.map((x,i)=><div key={i} className="flex flex-wrap justify-between gap-2 py-3 text-sm"><span>{x.text}</span><span className="text-xs text-[#a39081]">{new Date(x.createdAt).toLocaleString()}</span></div>):<p className="text-xs text-[#a39081]">No recent activity.</p>}</div></section></div>}
